From: QbProg <tholag@gmail.com>
Date: Thu, 12 May 2011 21:01:43 +0200
Subject: [PATCH 26/82] [warning-fix][empty-conditional-body]

Fixes warning like : found ';' after conditional instruction. Is what you want?

Cases like
if (a);
---
 src/TCollection/TCollection_ExtendedString.cxx |    2 ++
 1 files changed, 2 insertions(+), 0 deletions(-)

From b73a3bebd710867d92b93d62f76e05e881f32f21 Mon Sep 17 00:00:00 2001
From: tpaviot <tpaviot@gmail.com>
Date: Thu, 20 Oct 2011 22:01:47 +0200
Subject: [PATCH] [warning-fix][empty-body]

---
 inc/LDOM_DeclareSequence.hxx                 |    2 +-
 inc/Standard_String.hxx                      |   16 ++++++++--------
 src/Adaptor3d/Adaptor3d_TopolTool.cxx        |    4 ++--
 src/AlienImage/AlienImage_GIFLZWDict.cxx     |    2 +-
 src/BOP/BOP_ArgumentAnalyzer.cxx             |    2 +-
 src/BOP/BOP_ShellSolid_3.cxx                 |    2 +-
 src/BRepBuilderAPI/BRepBuilderAPI_Sewing.cxx |    4 ++--
 src/FEmTool/FEmTool_Assembly.cxx             |    8 ++++----
 src/FEmTool/FEmTool_ProfileMatrix.cxx        |    2 +-
 src/IGESDefs/IGESDefs_ToolMacroDef.cxx       |    2 +-
 src/LDOM/LDOM_XmlWriter.cxx                  |    2 +-
 src/MgtTopoDS/MgtTopoDS.cxx                  |    2 +-
 src/PlotMgt/PlotMgt_fillpolyarc.cxx          |    1 -
 src/PlotMgt/PlotMgt_fillpolygon.cxx          |    4 ----
 src/RWStl/RWStl.cxx                          |    2 +-
 src/Resource/Resource_Manager.cxx            |    2 +-
 src/ShapeFix/ShapeFix_ComposeShell.cxx       |    2 +-
 src/ShapeUpgrade/ShapeUpgrade_WireDivide.cxx |    4 ++--
 src/StepData/StepData_StepWriter.cxx         |    6 +++---
 19 files changed, 32 insertions(+), 37 deletions(-)

Index: oce/src/Adaptor3d/Adaptor3d_TopolTool.cxx
===================================================================
--- oce.orig/src/Adaptor3d/Adaptor3d_TopolTool.cxx
+++ oce/src/Adaptor3d/Adaptor3d_TopolTool.cxx
@@ -1157,7 +1157,7 @@ void Adaptor3d_TopolTool::BSplSamplePnts
     else { //myNbSamplesU == 3
       //insert in bigger segment
       i = 2;
-      while(!anUFlg(i++));
+      while(!anUFlg(i++)) {}
       if(i < nbsu/2) j = Min(i+(nbsu-i)/2, nbsu-1);
       else j = Max(i/2, 2);
     }
@@ -1242,7 +1242,7 @@ void Adaptor3d_TopolTool::BSplSamplePnts
     else { //myNbSamplesU == 3
       //insert in bigger segment
       i = 2;
-      while(!aVFlg(i++));
+      while(!aVFlg(i++)) {}
       if(i < nbsv/2) j = Min(i+(nbsv-i)/2, nbsv-1);
       else j = Max(i/2, 2);
     }
Index: oce/src/BRepBuilderAPI/BRepBuilderAPI_Sewing.cxx
===================================================================
--- oce.orig/src/BRepBuilderAPI/BRepBuilderAPI_Sewing.cxx
+++ oce/src/BRepBuilderAPI/BRepBuilderAPI_Sewing.cxx
@@ -2928,7 +2928,7 @@ void BRepBuilderAPI_Sewing::VerticesAsse
 #ifdef DEB
       cout << "Assemble " << nbVert << " vertices on faces..." << endl;
 #endif
-      while (GlueVertices(myVertexNode,myNodeSections,myBoundFaces,myTolerance, thePI));
+      while (GlueVertices(myVertexNode,myNodeSections,myBoundFaces,myTolerance, thePI)) {}
     }
     if (!aPS.More())
       return;
@@ -2937,7 +2937,7 @@ void BRepBuilderAPI_Sewing::VerticesAsse
 #ifdef DEB
       cout << "Assemble " << nbVertFree << " vertices on floating edges..." << endl;
 #endif
-      while (GlueVertices(myVertexNodeFree,myNodeSections,myBoundFaces,myTolerance, thePI));
+      while (GlueVertices(myVertexNodeFree,myNodeSections,myBoundFaces,myTolerance, thePI)) {}
     }
   }
 }
Index: oce/src/FEmTool/FEmTool_Assembly.cxx
===================================================================
--- oce.orig/src/FEmTool/FEmTool_Assembly.cxx
+++ oce/src/FEmTool/FEmTool_Assembly.cxx
@@ -538,7 +538,7 @@ void FEmTool_Assembly::AddConstraint(con
 	for(i = Aux1->Lower(); i <= Aux1->Upper(); i++) Coeff->SetValue(i, Aux1->Value(i));
 	for(i = Aux1->Upper() + 1; i <= Imax; i++) Coeff->SetValue(i, 0.);
 	Iter.Initialize(L);
-	for(i = 1; i < s1; Iter.Next(), i++);
+	for(i = 1; i < s1; Iter.Next(), i++) {}
 	Iter.Value() = Coeff;
       }
       else if(Imin > Aux1->Upper() && Imax >= Aux2->Lower()) { 
@@ -547,7 +547,7 @@ void FEmTool_Assembly::AddConstraint(con
 	for(i = Imin; i <= Aux2->Lower() - 1; i++) Coeff->SetValue(i, 0.);
 	for(i = Aux2->Lower(); i <= Aux2->Upper(); i++) Coeff->SetValue(i, Aux2->Value(i));
 	Iter.Initialize(L);
-	for(i = 1; i < s2; Iter.Next(), i++);
+	for(i = 1; i < s2; Iter.Next(), i++) {}
 	Iter.Value() = Coeff;
      }
       else if(Imin > Aux1->Upper() && Imax < Aux2->Lower()) {
@@ -555,7 +555,7 @@ void FEmTool_Assembly::AddConstraint(con
 	Coeff = new TColStd_HArray1OfReal(Imin,Imax);
 	Coeff->Init(0.);
 	Iter.Initialize(L);
-	for(i = 1; i < s1; Iter.Next(), i++);
+	for(i = 1; i < s1; Iter.Next(), i++) {}
 	L.InsertAfter(Coeff,Iter);
       }
       else {
@@ -565,7 +565,7 @@ void FEmTool_Assembly::AddConstraint(con
 	for(i = Aux1->Upper() + 1; i <= Aux2->Lower() - 1; i++) Coeff->SetValue(i, 0.);
 	for(i = Aux2->Lower(); i <= Aux2->Upper(); i++) Coeff->SetValue(i, Aux2->Value(i));
 	Iter.Initialize(L);
-	for(i = 1; i < s1; Iter.Next(), i++);
+	for(i = 1; i < s1; Iter.Next(), i++) {}
 	Iter.Value() = Coeff;
 	Iter.Next();
 	L.Remove(Iter);
Index: oce/src/FEmTool/FEmTool_ProfileMatrix.cxx
===================================================================
--- oce.orig/src/FEmTool/FEmTool_ProfileMatrix.cxx
+++ oce/src/FEmTool/FEmTool_ProfileMatrix.cxx
@@ -40,7 +40,7 @@ FEmTool_ProfileMatrix::FEmTool_ProfileMa
  
   for(i = 1, k = 1; i <= FirstIndexes.Length(); i++)
     for(j = FirstIndexes(i); j <= i; j++) {
-      for(l = i+1; l <= FirstIndexes.Length() && j < FirstIndexes(l); l++);
+      for(l = i+1; l <= FirstIndexes.Length() && j < FirstIndexes(l); l++) {}
 
       if(l > FirstIndexes.Length()) NextCoeff->SetValue(k, 0);
       else NextCoeff->SetValue(k, l);
Index: oce/src/IGESDefs/IGESDefs_ToolMacroDef.cxx
===================================================================
--- oce.orig/src/IGESDefs/IGESDefs_ToolMacroDef.cxx
+++ oce/src/IGESDefs/IGESDefs_ToolMacroDef.cxx
@@ -46,7 +46,7 @@ void  IGESDefs_ToolMacroDef::ReadOwnPara
   // Counting the no. of language statements.
   Standard_Integer nbval; // svv Jan 10 2000 : porting on DEC
   for (nbval = 0; PR.CurrentNumber() != PR.NbParams();
-       nbval++, PR.SetCurrentNumber(PR.CurrentNumber() + 1));
+       nbval++, PR.SetCurrentNumber(PR.CurrentNumber() + 1)) {}
 
   PR.SetCurrentNumber(tempCurrent);
   if (nbval > 0) langStatements =
Index: oce/src/LDOM/LDOM_DeclareSequence.hxx
===================================================================
--- oce.orig/src/LDOM/LDOM_DeclareSequence.hxx
+++ oce/src/LDOM/LDOM_DeclareSequence.hxx
@@ -212,7 +212,7 @@ void ClassName::InsertAt (const Standard
       if (anItem -> myPrev) anItem -> myPrev -> myNext = anItem;              \
       myLength++;                                                             \
       myICur++;                                                               \
-   } else ;                                                                   \
+   }                                                                          \
 }                                                                             \
                                                                               \
 void ClassName::Clear ()                                                      \
Index: oce/src/LDOM/LDOM_XmlWriter.cxx
===================================================================
--- oce.orig/src/LDOM/LDOM_XmlWriter.cxx
+++ oce/src/LDOM/LDOM_XmlWriter.cxx
@@ -164,7 +164,7 @@ static LXMLCh * getEncodingName (const L
     anEncoding = anUTFEncoding;
   }
   Standard_Integer aLen = 0;
-  while (anEncoding[aLen++] != chNull);
+  while (anEncoding[aLen++] != chNull) {}
   LXMLCh * aResult = new LXMLCh [aLen];
   memcpy (aResult, anEncoding, aLen * sizeof (LXMLCh));
   return aResult;
Index: oce/src/MgtTopoDS/MgtTopoDS.cxx
===================================================================
--- oce.orig/src/MgtTopoDS/MgtTopoDS.cxx
+++ oce/src/MgtTopoDS/MgtTopoDS.cxx
@@ -372,7 +372,7 @@ void MgtTopoDS::Translate1
     // count the number of <sub-shape> of the Shape's TShape
     Standard_Integer nbElem = 0;
     TopoDS_Iterator itr(S);
-    for (;itr.More();++nbElem,itr.Next());
+    for (;itr.More();++nbElem,itr.Next()) {}
 
     if (nbElem) {
       Handle(PTopoDS_HArray1OfShape1) myArray = 
Index: oce/src/RWStl/RWStl.cxx
===================================================================
--- oce.orig/src/RWStl/RWStl.cxx
+++ oce/src/RWStl/RWStl.cxx
@@ -435,7 +435,7 @@ Handle_StlMesh_Mesh RWStl::ReadAscii (co
   rewind(file);
 
   // skip header
-  while (getc(file) != '\n');
+  while (getc(file) != '\n') {}
 #ifdef DEB
   cout << "start mesh\n";
 #endif
Index: oce/src/Resource/Resource_Manager.cxx
===================================================================
--- oce.orig/src/Resource/Resource_Manager.cxx
+++ oce/src/Resource/Resource_Manager.cxx
@@ -179,7 +179,7 @@ static Standard_Integer WhatKindOfLine(O
   if (!Pos2 || Pos1 == Pos2)
     return ERROR;
 
-  for (Pos = Pos2-1; Line.Value(Pos) == '\t' || Line.Value(Pos) == ' ' ; Pos--);
+  for (Pos = Pos2-1; Line.Value(Pos) == '\t' || Line.Value(Pos) == ' ' ; Pos--) {}
   aToken1 = Line.SubString(Pos1, Pos);
 
   if (Debug)
Index: oce/src/ShapeFix/ShapeFix_ComposeShell.cxx
===================================================================
--- oce.orig/src/ShapeFix/ShapeFix_ComposeShell.cxx
+++ oce/src/ShapeFix/ShapeFix_ComposeShell.cxx
@@ -725,7 +725,7 @@ static void DistributeSplitPoints (const
     params.SetValue ( i, ( isreversed ? l : f ) );
   }
   
-  for ( i=1; i <= indexes.Length() && indexes(i) < index; i++ );
+  for ( i=1; i <= indexes.Length() && indexes(i) < index; i++ ) {}
   for ( Standard_Integer shift = 1; i <= indexes.Length() && indexes(i) == index; i++ ) {
     while (  shift < nsplit  && isreversed != ( values(i) > params(shift) ) ) shift++;
     indexes.SetValue ( i, index + shift - 1 );
Index: oce/src/ShapeUpgrade/ShapeUpgrade_WireDivide.cxx
===================================================================
--- oce.orig/src/ShapeUpgrade/ShapeUpgrade_WireDivide.cxx
+++ oce/src/ShapeUpgrade/ShapeUpgrade_WireDivide.cxx
@@ -224,7 +224,7 @@ static void CorrectSplitValues(const Han
   }
   if(new2d->Value(len3d) > Last3d) {
     Standard_Integer ind; // svv #1
-    for( ind = len3d; ind > 1 && !fixNew2d(ind); ind--);
+    for( ind = len3d; ind > 1 && !fixNew2d(ind); ind--) {}
     Standard_Real lastFix = new2d->Value(ind);
     for(i = len3d; i >= ind; i--) { 
       new2d->SetValue(i,lastFix);
@@ -249,7 +249,7 @@ static void CorrectSplitValues(const Han
   }
   if(new3d->Value(len2d) > Last2d) {
     Standard_Integer ind; // svv #1
-    for(ind = len2d; ind > 1 && !fixNew3d(ind); ind--);
+    for(ind = len2d; ind > 1 && !fixNew3d(ind); ind--) {}
     Standard_Real lastFix = new3d->Value(ind);
     for(i = len2d; i >= ind; i--) { 
       new3d->SetValue(i,lastFix);
Index: oce/src/StepData/StepData_StepWriter.cxx
===================================================================
--- oce.orig/src/StepData/StepData_StepWriter.cxx
+++ oce/src/StepData/StepData_StepWriter.cxx
@@ -815,13 +815,13 @@ void StepData_StepWriter::Send (const TC
 	  break;
 	}
 	Standard_Integer stop = StepLong; // position of last separator
-	for ( ; stop > 0 && aval.Value(stop) != ' '; stop-- );
+	for ( ; stop > 0 && aval.Value(stop) != ' '; stop-- ) {}
 	if ( ! stop ) {
 	  stop = StepLong;
-	  for ( ; stop > 0 && aval.Value(stop) != '\\'; stop-- );
+	  for ( ; stop > 0 && aval.Value(stop) != '\\'; stop-- ) {}
 	  if ( ! stop ) {
 	    stop = StepLong;
-	    for ( ; stop > 0 && aval.Value(stop) != '_'; stop-- );
+		for ( ; stop > 0 && aval.Value(stop) != '_'; stop-- ) {}
 	    if ( ! stop ) stop = StepLong;
 	  }
 	}
